// When the frontend is hosted separately from the backend (e.g. Netlify + Render),
// set this to the deployed backend's API URL. For local dev / same-origin hosting,
// the relative '/api' path works as-is.
window.HIMSTORE_API_BASE = window.HIMSTORE_API_BASE || 'https://hp-ecom.onrender.com/api';
